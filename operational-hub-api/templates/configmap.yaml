apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "operational-hub-api.name" . }}-config
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "operational-hub-api.labels" . | nindent 4 }}
data:
  {{- range $name, $value := .Values.config }}
  {{ $name }}: {{ $value | quote }}
  {{- end }}
