{{- if .Values.secretsManager.enabled }}
apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: {{ include "java-service.name" . }}-spc
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "java-service.labels" . | nindent 4 }}
spec:
  provider: aws
  secretObjects:
    - secretName: {{ include "java-service.secretName" . }}
      type: Opaque
      data:
        {{- range $secretKey := .Values.secretsManager.keys }}
        - objectName: {{ $secretKey }}
          key: {{ $secretKey }}
        {{- end }}
  parameters:
    objects: |
      - objectName: "{{ .Values.secretsManager.awsSecretName }}"
        objectType: "secretsmanager"
        jmesPath:
          {{- range $secretKey := .Values.secretsManager.keys }}
          - path: {{ $secretKey }}
            objectAlias: {{ $secretKey }}
          {{- end }}
{{- end }}
