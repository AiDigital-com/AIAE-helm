apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "java-service.name" . }}-config
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "java-service.labels" . | nindent 4 }}
data:
  {{- range $name, $value := .Values.config }}
  {{ $name }}: {{ $value | quote }}
  {{- end }}
