apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "aiae-paicing-api.name" . }}-config
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "aiae-paicing-api.labels" . | nindent 4 }}
data:
  {{- range $name, $value := .Values.config }}
  {{ $name }}: {{ $value | quote }}
  {{- end }}
